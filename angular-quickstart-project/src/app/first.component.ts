import { Component } from "@angular/core";

@Component({
    selector: 'app-first',
    template: `<h2>First Component</h2>
    <p>This is the first component</p>`,
    styles: [`h2 { color: navy; }
    p { color: marron; font-weight: bold}` ]
})

export class FirstComponent {

    // Logics Here

}